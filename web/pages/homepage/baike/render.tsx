import React from "react";

import { SProps } from "ssr-types-react";

export default (props: SProps) => {
    return (
        <>
            <span>[列表]</span>
            <span>这里是homepage/baike</span>
        </>
    );
}
