import React from "react";

import { SProps } from "ssr-types-react";

export default (props: SProps) => {
    return (
        <>
            <span>[漫画详情]</span>
            <span>这里是homepage/comiclist/:comicId</span>
        </>
    );
}
