import React from "react";

import { SProps } from "ssr-types-react";

export default (props: SProps) => {

    return (
        <>
           <span>这里是homepage/usercenter/:userId/activity</span>

        </>
    );
}
