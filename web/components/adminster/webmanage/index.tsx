import React from "react";

import { SProps } from "ssr-types-react";

export default (props: SProps) => {
    return (
        <>
            <span>[后台-源站管理]</span>
            <span>这里是homepage/administer/webmanage</span>
        </>
    );
}
