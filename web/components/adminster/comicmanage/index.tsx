import React from "react";

import { SProps } from "ssr-types-react";

export default (props: SProps) => {
    return (
        <>
            <span>[后台-漫画管理]</span>
            <span>这里是homepage/administer/comicmanage</span>
        </>
    );
}
